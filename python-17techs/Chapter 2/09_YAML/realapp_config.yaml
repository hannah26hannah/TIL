definitions:
  default: &default
    min_log_level: info
    app_name: realapp
    log_dir: logs
    secure_mode: false
Configurations:
  dev: *default
  qa: *default
  production: *default
